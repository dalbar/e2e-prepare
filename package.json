{
  "name": "vue-sample",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5050",
    "start": "serve dist -l ${PORT:-8080}"
  },
  "dependencies": {
    "vue": "^3.2.30"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.1.0",
    "serve": "^13.0.2",
    "vite": "^2.7.13"
  }
}
